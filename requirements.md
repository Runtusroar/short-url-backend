# 短链系统需求文档

## 1. 技术栈
- 后端：FastAPI + PostgreSQL + SQLAlchemy 2.0 + Alembic
- 前端：React
- 部署：Docker Compose
- 测试：pytest + httpx

## 2. 短链核心功能
- 短码：固定 6 位随机字符串，支持自定义别名（custom alias）。
- CRUD：创建、读取、更新、删除短链。
- 一个短链可关联多个长链接，分两类：
  - 允许访问后跳转的长链接；
  - 不允许访问后跳转的长链接。
- 命中规则：允许/不允许的长链接均按权重随机选择。

## 3. 访问控制
- 可按国家、UA 平台、Referer 配置访问规则。
- 规则按优先级顺序匹配，第一条命中的规则生效。
- 无规则命中时默认允许访问。
- 支持全局 IP 黑名单。

## 4. 限流
- 对短链跳转接口做访问频次限制，防止滥用与刷量。
- 限流维度：按请求 IP + 短码，或仅按请求 IP。
- 超出限制后返回 429 Too Many Requests。

## 5. 访问日志
- 记录信息：IP、UA、Referer、国家、UA 平台、命中结果、目标 URL。
- 每个短链按 +8 时区展示每日访问记录。
- 保留原始访问日志。

## 6. 用户系统
- 角色：
  - **admin**：管理所有短链、用户、日志、黑名单；
  - **operator**：管理自己的短链与日志、维护黑名单；
  - **client**：只读查看被授权给自己的短链与日志，不能修改。
- 登录方式：用户名 + 密码（JWT）。
- admin/operator 可将短链授权给指定 client 查看。

## 7. 部署与开发
- 使用 docker-compose 一键启动。
- Makefile 提供常用快捷命令（测试、迁移、启动等）。
- MaxMind GeoIP 数据库通过官方 geoipupdate 容器自动更新。
